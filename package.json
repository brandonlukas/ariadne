{
  "name": "ariadne",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node src/metrics.test.ts && node src/api.test.ts"
  },
  "dependencies": {
    "d3-force": "^3.0.0"
  },
  "devDependencies": {
    "@types/d3-force": "^3.0.10",
    "typescript": "^5.5.0",
    "vite": "^6.0.0"
  }
}
